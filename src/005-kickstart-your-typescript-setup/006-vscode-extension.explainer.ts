// Explain the VSCode extension

// const num: number = "asdawd";

// In the above code the IDE checks the type of the variable assigned to `num` against the type `number` which was used to declare the variable and flags a type error 

// const num: string = "asdawd"
const num: number = 10_00_000
console.log(num)